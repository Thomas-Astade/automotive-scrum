//  $0 - name of the baseline
//  $1 - The baselined artefact
//  $2 - the general activity

// expect a folder tags

extend folder tags {
    contains baseline;
}

artefact baseline {
    label $0 + " baseline";
}

activity doBaseline {
    label "do a " + $0 + " baseline";
    transform $1;
    create baseline;
    specialize $2;
}
